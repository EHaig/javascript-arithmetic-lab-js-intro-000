function add(a, b) {
  return parseInt(a) + parseInt(b);
}

function subtract(){
  return parseInt(a) - parseInt(b);
}

function multiply(){
  return parseInt(a) * parseInt(b);
}

function divide(){
return parseInt(a) / parseInt(b);
}
